# Local execution defaults for shell exports or IDE run configurations.
# The framework reads system properties first, but these names align with GitHub Actions secrets.

API_AUTH_TYPE=none
API_AUTH_TOKEN=
API_AUTH_USERNAME=
API_AUTH_PASSWORD=

BROWSER=chromium
HEADLESS=true
TEST_ENV=local
